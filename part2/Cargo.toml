[package]
name = "haversine-generator"
version = "0.1.0"
edition = "2024"
rust-version = "1.94"
publish = false

[features]
# Record and print internal phase timings; disabled in ordinary builds.
profiling = []
# Also exercises the fallback clock on hosts with a specialized timer.
portable-timer = []
