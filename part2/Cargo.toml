[package]
name = "haversine-generator"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
tracing = "0.1"
tracing-chrome = "0.7"
tracing-subscriber = { version = "0.3", default-features = false, features = ["registry"] }
